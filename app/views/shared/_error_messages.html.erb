
<% if @task.errors.any? %>
	<div  id="error_explanation">
		<div class="alert alert-error">
			The count error messagess: <%= pluralize(@task.errors.count,"error") %>
		</div>
		<ul>
			<% @task.errors.full_messages.each do |msg| %>
				<li>* <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>
